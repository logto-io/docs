<table>
  <thead>
    <tr>
      <td>
        <strong>Property Name</strong>
      </td>
      <td>
        <strong>Property Description</strong>
      </td>
      <td>
        <strong>Required</strong>
      </td>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>API Name</td>
      <td>A user friendly name that can help you to identify the API resource.</td>
      <td>true</td>
    </tr>
    <tr>
      <td>API Identifier</td>
      <td>
        The unique <a href="https://www.rfc-editor.org/rfc/rfc8707.html">API Resource Indicator</a>{' '}
        value, with restrictions listed above.
        <br />
        Logto will use it to identify the requested API resource and grant the authorization tokens
        accordingly.
      </td>
      <td>true</td>
    </tr>
    <tr>
      <td>
        Token expiration time <i>(in seconds)</i>
      </td>
      <td>
        The expiration time set for <strong>access_token</strong>. The default expiration value is{' '}
        <strong>3600</strong>.
      </td>
      <td>false</td>
    </tr>
    <tr>
      <td>Default API</td>
      <td>
        Only zero or one default API can be set per tenant.
        <br />
        When a default API is designated, the resource parameter can be omitted in the auth request.
        Subsequent token exchanges will use that API as the audience by default, resulting in the
        issuance of JWTs.
      </td>
      <td>false</td>
    </tr>
  </tbody>
</table>

:::caution
Requests with a expired <strong>access_token</strong> should be restricted.
This is implemented to protect your API from a abused token.
The larger value is set, the longer will a issued <strong>access_token</strong> survive, and the more vulnerable your API will be.
:::
