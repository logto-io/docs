You need to have an access token that satisfies the following criteria:

<ul>
  <li>It&apos;s a valid token</li>
  <li>It&apos;s issued by your Logto instance</li>
  <li>
    Its audience (the resource indicator) points to <code>https://api.logto.io</code>
  </li>
  {/* eslint-disable-next-line no-undef */}
  {props.children && <li>{props.children}</li>}
</ul>
