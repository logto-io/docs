---
sidebar_label: FastAPI
---

import AddValidationLogic from '../../../authorization/validate-access-tokens/python/_add-validation-logic.mdx';
import Init from '../../../authorization/validate-access-tokens/python/_init.mdx';
import ApplyMiddleware from '../../../authorization/validate-access-tokens/python/fragments/fastapi/_apply-middleware.md';
import MainContent from '../../fragments/_main-content.mdx';

import InitProject from './_init-project.mdx';

# Protect your FastAPI with RBAC and JWT validation

This guide will help you implement authorization to secure your FastAPI APIs using [Role-based access control (RBAC)](/authorization/role-based-access-control) and [JSON Web Tokens (JWTs)](https://auth.wiki/jwt) issued by Logto.

{/* WARNING: We cannot use variables in the headings or introductory text since it will not be rendered correctly. */}

<MainContent
  framework="fastapi"
  initializeProject={<InitProject />}
  languageInit={<Init />}
  applyMiddleware={<ApplyMiddleware />}
  addValidationLogic={<AddValidationLogic framework="fastapi" />}
/>
