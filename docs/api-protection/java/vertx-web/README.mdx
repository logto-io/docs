---
sidebar_label: Vert.x Web
---

import AddValidationLogic from '../../../authorization/validate-access-tokens/java/_add-validation-logic.mdx';
import Init from '../../../authorization/validate-access-tokens/java/_init.mdx';
import ApplyMiddleware from '../../../authorization/validate-access-tokens/java/fragments/vertx-web/_apply-middleware.md';
import MainContent from '../../fragments/_main-content.mdx';

import InitProject from './_init-project.mdx';

# Protect your Vert.x Web API with RBAC and JWT validation

This guide will help you implement authorization to secure your Vert.x Web APIs using [Role-based access control (RBAC)](/authorization/role-based-access-control) and [JSON Web Tokens (JWTs)](https://auth.wiki/jwt) issued by Logto.

{/* WARNING: We cannot use variables in the headings or introductory text since it will not be rendered correctly. */}

<MainContent
  framework="vertx-web"
  initializeProject={<InitProject />}
  languageInit={<Init />}
  applyMiddleware={<ApplyMiddleware />}
  addValidationLogic={<AddValidationLogic framework="vertx-web" />}
/>
