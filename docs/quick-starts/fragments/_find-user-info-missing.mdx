You may find some user information are missing in the returned object from <code>{props.method}</code>. This is because OAuth 2.0 and OpenID Connect (OIDC) are designed to follow the principle of least privilege (PoLP), and Logto is built on top of these standards.
