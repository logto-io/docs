{props.type ? null : (() => {throw new Error(`props.type is required when importing _claims-need-network-request.mdx`)})()}

### 需要网络请求的声明 \{#claims-that-need-network-requests}

为了防止 ID 令牌 (ID token) 过大，一些声明需要通过网络请求来获取。例如，即使在权限中请求了 `custom_data` 声明，它也不会包含在用户对象中。要访问这些声明，<span>{props.type === 'method' && (<>你可以使用 <code>{props.method}</code> 方法</>)}{props.type === 'option' && <>你可以配置 <code>{props.configOption}</code> 选项</>}</span>：

{props.codeSnippet}

{props.type === 'method' && (

<>
  该方法将通过请求{' '}
  <a href="https://openid.net/specs/openid-connect-core-1_0.html#UserInfo">userinfo 端点</a>
  来获取用户信息。要了解更多可用的权限和声明，请参阅 <a href="#scopes-and-claims">权限和声明</a>
  部分。
</>
)}

{props.type === 'option' && (

<>
  通过配置 <code>{props.configOption}</code>，SDK 将在用户登录后通过请求{' '}
  <a href="https://openid.net/specs/openid-connect-core-1_0.html#UserInfo">userinfo 端点</a>{' '}
  来获取用户信息，并且一旦请求完成，<code>{props.value ?? 'user.custom_data'}</code> 将可用。
</>
)}
