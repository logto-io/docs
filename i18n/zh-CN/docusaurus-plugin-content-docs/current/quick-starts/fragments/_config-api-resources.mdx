一旦你设置了 API 资源，就可以在应用中配置 Logto 时添加它们：

{props.configResourcesCode}

每个 API 资源都有其自己的权限（权限）。

例如，`https://shopping.your-app.com/api` 资源具有 `shopping:read` 和 `shopping:write` 权限，而 `https://store.your-app.com/api` 资源具有 `store:read` 和 `store:write` 权限。

要请求这些权限，你可以在应用中配置 Logto 时添加它们：

{props.configResourcesWithScopesCode}

你可能会注意到权限是与 API 资源分开定义的。这是因为 [OAuth 2.0 的资源指示器](https://www.rfc-editor.org/rfc/rfc8707.html) 指定请求的最终权限将是所有目标服务中所有权限的笛卡尔积。

{props.configResourcesWithSharedScopesCode &&

<>

<p>
  因此，在上述情况下，权限可以从 Logto 中的定义简化，两个 API 资源都可以拥有 <code>read</code> 和{' '}
  <code>write</code> 权限，而无需前缀。然后，在 Logto 配置中：
</p>

{props.configResourcesWithSharedScopesCode}

<p>
  对于每个 API 资源，它将请求 <code>read</code> 和 <code>write</code> 权限。
</p>

</>
}

:::note
请求 API 资源中未定义的权限是可以的。例如，即使 API 资源没有可用的 `email` 权限，你也可以请求 `email` 权限。不可用的权限将被安全地忽略。
:::

成功登录后，Logto 将根据用户的角色向 API 资源发布适当的权限。
