如果你对组织不熟悉，请阅读 [🏢 组织（多租户）](/organizations) 以开始了解。

在配置 Logto 客户端时，你需要添加 <code>{props.organizationScope}</code> 权限：

{props.configOrganizationCode}

用户登录后，你可以获取用户的组织令牌：

{props.getOrganizationAccessTokenCode}
