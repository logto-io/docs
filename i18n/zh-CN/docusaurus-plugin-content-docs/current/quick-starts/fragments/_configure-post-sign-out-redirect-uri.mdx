<p>
  让我们切换到 Logto Console 的应用详情页面。添加一个注销后重定向 URI{' '}
  <code>{props.postSignOutRedirectUri}</code>，然后点击“保存更改”。
</p>

<img alt="Logto Console 中的注销后重定向 URI" src={props.figureSrc} width="600px" />

[注销后重定向 URI](https://openid.net/specs/openid-connect-rpinitiated-1_0.html#RPLogout) 是一个 OAuth 2.0 概念，意味着在注销后应该重定向的位置。
