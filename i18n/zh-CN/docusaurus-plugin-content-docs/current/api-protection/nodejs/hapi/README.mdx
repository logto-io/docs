---
sidebar_label: Hapi.js
---

import AddValidationLogic from '../../../authorization/validate-access-tokens/nodejs/_add-validation-logic.mdx';
import Init from '../../../authorization/validate-access-tokens/nodejs/_init.mdx';
import ApplyMiddleware from '../../../authorization/validate-access-tokens/nodejs/fragments/hapi/_apply-middleware.md';
import MainContent from '../../fragments/_main-content.mdx';

import InitProject from './_init-project.mdx';

# 使用基于角色的访问控制 (RBAC) 和 JWT 验证保护你的 Hapi.js API

本指南将帮助你通过使用 Logto 颁发的 [基于角色的访问控制 (RBAC)](/authorization/role-based-access-control) 和 [JSON Web Token (JWT)](https://auth.wiki/jwt) 来实现授权 (Authorization)，以保护你的 Hapi.js API。

{/* 警告：我们不能在标题或介绍性文本中使用变量，因为它们不会被正确渲染。 */}

<MainContent
  framework="hapi"
  initializeProject={<InitProject />}
  languageInit={<Init />}
  applyMiddleware={<ApplyMiddleware />}
  addValidationLogic={<AddValidationLogic framework="hapi" />}
/>
