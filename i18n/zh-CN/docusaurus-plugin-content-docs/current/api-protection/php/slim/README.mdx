---
sidebar_label: Slim
---

import AddValidationLogic from '../../../authorization/validate-access-tokens/php/_add-validation-logic.mdx';
import Init from '../../../authorization/validate-access-tokens/php/_init.mdx';
import ApplyMiddleware from '../../../authorization/validate-access-tokens/php/fragments/slim/_apply-middleware.md';
import MainContent from '../../fragments/_main-content.mdx';

import InitProject from './_init-project.mdx';

# 使用基于角色的访问控制 (RBAC) 和 JWT 验证保护你的 Slim API

本指南将帮助你通过实现授权 (Authorization)，结合 [基于角色的访问控制 (RBAC)](/authorization/role-based-access-control) 和由 Logto 签发的 [JSON Web Token (JWT)](https://auth.wiki/jwt)，来保护你的 Slim API。

{/* 警告：我们不能在标题或介绍性文本中使用变量，因为它不会被正确渲染。 */}

<MainContent
  framework="slim"
  initializeProject={<InitProject />}
  languageInit={<Init />}
  applyMiddleware={<ApplyMiddleware />}
  addValidationLogic={<AddValidationLogic framework="slim" />}
/>
