---
sidebar_label: Micronaut
---

import AddValidationLogic from '../../../authorization/validate-access-tokens/java/_add-validation-logic.mdx';
import Init from '../../../authorization/validate-access-tokens/java/_init.mdx';
import ApplyMiddleware from '../../../authorization/validate-access-tokens/java/fragments/micronaut/_apply-middleware.md';
import MainContent from '../../fragments/_main-content.mdx';

import InitProject from './_init-project.mdx';

# 使用基于角色的访问控制 (RBAC) 和 JWT 验证保护你的 Micronaut API

本指南将帮助你通过使用 Logto 颁发的 [基于角色的访问控制 (RBAC)](/authorization/role-based-access-control) 和 [JSON Web Token (JWT)](https://auth.wiki/jwt) 来实现授权 (Authorization)，从而保护你的 Micronaut API。

{/* 警告：我们不能在标题或介绍文本中使用变量，否则无法正确渲染。 */}

<MainContent
  framework="micronaut"
  initializeProject={<InitProject />}
  languageInit={<Init />}
  applyMiddleware={<ApplyMiddleware />}
  addValidationLogic={<AddValidationLogic framework="micronaut" />}
/>
