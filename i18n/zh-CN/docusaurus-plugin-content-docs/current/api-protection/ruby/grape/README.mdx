---
sidebar_label: Grape
---

import AddValidationLogic from '../../../authorization/validate-access-tokens/ruby/_add-validation-logic.mdx';
import Init from '../../../authorization/validate-access-tokens/ruby/_init.mdx';
import ApplyMiddleware from '../../../authorization/validate-access-tokens/ruby/fragments/grape/_apply-middleware.md';
import MainContent from '../../fragments/_main-content.mdx';

import InitProject from './_init-project.mdx';

# 使用基于角色的访问控制 (RBAC) 和 JWT 验证保护你的 Grape API

本指南将帮助你通过 [基于角色的访问控制 (RBAC)](/authorization/role-based-access-control) 和 Logto 签发的 [JSON Web Token (JWT)](https://auth.wiki/jwt) 实现授权 (Authorization)，以保护你的 Grape API。

{/* 警告：我们不能在标题或介绍文本中使用变量，否则无法正确渲染。 */}

<MainContent
  framework="grape"
  initializeProject={<InitProject />}
  languageInit={<Init />}
  applyMiddleware={<ApplyMiddleware />}
  addValidationLogic={<AddValidationLogic framework="grape" />}
/>
