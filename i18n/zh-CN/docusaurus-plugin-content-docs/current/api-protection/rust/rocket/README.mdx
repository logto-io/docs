---
sidebar_label: Rocket
---

import AddValidationLogic from '../../../authorization/validate-access-tokens/rust/_add-validation-logic.mdx';
import Init from '../../../authorization/validate-access-tokens/rust/_init.mdx';
import ApplyMiddleware from '../../../authorization/validate-access-tokens/rust/fragments/rocket/_apply-middleware.md';
import MainContent from '../../fragments/_main-content.mdx';

import InitProject from './_init-project.mdx';

# 使用基于角色的访问控制 (RBAC) 和 JWT 验证保护你的 Rocket API

本指南将帮助你通过使用 [基于角色的访问控制 (RBAC)](/authorization/role-based-access-control) 和 Logto 签发的 [JSON Web Token (JWT)](https://auth.wiki/jwt) 来实现 Rocket API 的授权 (Authorization) 安全防护。

{/* 警告：我们不能在标题或介绍文本中使用变量，否则不会正确渲染。 */}

<MainContent
  framework="rocket"
  initializeProject={<InitProject />}
  languageInit={<Init />}
  applyMiddleware={<ApplyMiddleware />}
  addValidationLogic={<AddValidationLogic framework="rocket" />}
/>
