---
sidebar_label: Axum
---

import AddValidationLogic from '../../../authorization/validate-access-tokens/rust/_add-validation-logic.mdx';
import Init from '../../../authorization/validate-access-tokens/rust/_init.mdx';
import ApplyMiddleware from '../../../authorization/validate-access-tokens/rust/fragments/axum/_apply-middleware.md';
import MainContent from '../../fragments/_main-content.mdx';

import InitProject from './_init-project.mdx';

# 使用基于角色的访问控制 (RBAC) 和 JWT 验证保护你的 Axum API

本指南将帮助你通过使用 [基于角色的访问控制 (RBAC)](/authorization/role-based-access-control) 和由 Logto 签发的 [JSON Web Token (JWT)](https://auth.wiki/jwt) 来实现授权 (Authorization)，从而保护你的 Axum API。

{/* 警告：我们不能在标题或介绍性文本中使用变量，否则无法正确渲染。 */}

<MainContent
  framework="axum"
  initializeProject={<InitProject />}
  languageInit={<Init />}
  applyMiddleware={<ApplyMiddleware />}
  addValidationLogic={<AddValidationLogic framework="axum" />}
/>
