:::info[前置条件]
在使用令牌交换授权（token exchange grant）之前，你需要为你的应用程序启用它：

1. 前往 <CloudLink to="/applications">控制台 > 应用程序</CloudLink> 并选择你的应用程序。
2. 在应用程序设置中，找到“令牌交换”部分。
3. 启用“允许令牌交换”开关。

出于安全原因，令牌交换默认是禁用的。如果你没有启用它，你将会收到“此应用程序不允许令牌交换”的错误。
:::
