成功创建 OAuth 凭证后，你将收到一个包含客户端 ID 和客户端密钥的提示模式窗口。

![Google Workspace 客户端凭证.webp](./assets/google_workspace_client_credentials.webp)

复制 `Client ID` 和 `Client secret`，并填写到 Logto 的 SSO 连接器 `Connection` 选项卡中的相应字段。

现在你已经在 Logto 上成功配置了一个 Google Workspace SSO 连接器。
