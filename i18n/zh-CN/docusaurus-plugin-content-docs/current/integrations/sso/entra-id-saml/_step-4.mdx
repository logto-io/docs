import azureAdAssignUsersImage from '@site/docs/integrations/sso/entra-id-saml/assets/azure_ad_assign_users.webp';

访问 Azure AD SSO 应用程序的 `Users and groups` 部分。点击 `Add user/group` 将用户分配到 Azure AD SSO 应用程序。只有分配到你的 Azure AD SSO 应用程序的用户才能通过 Azure AD SSO 连接器进行认证 (Authentication)。

<img src={azureAdAssignUsersImage} alt="Azure AD assign users" />
