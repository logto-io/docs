{props.type ? null : (() => {throw new Error(`props.type is required when importing _claims-need-network-request.mdx`)})()}

### ネットワークリクエストが必要なクレーム (Claims) \{#claims-that-need-network-requests}

ID トークンの肥大化を防ぐために、一部のクレーム (Claims) は取得するためにネットワークリクエストが必要です。例えば、`custom_data` クレームはスコープで要求されてもユーザーオブジェクトに含まれません。これらのクレームにアクセスするには、<span>{props.type === 'method' && (<> <code>{props.method}</code> メソッドを使用できます</>)}{props.type === 'option' && <> <code>{props.configOption}</code> オプションを設定できます</>}</span>：

{props.codeSnippet}

{props.type === 'method' && (

<>
  このメソッドは、
  <a href="https://openid.net/specs/openid-connect-core-1_0.html#UserInfo">
    userinfo エンドポイント
  </a>
  にリクエストを送信してユーザー情報を取得します。利用可能なスコープとクレームについて詳しくは、
  <a href="#scopes-and-claims">スコープとクレーム</a>のセクションを参照してください。
</>
)}

{props.type === 'option' && (

<>
  <code>{props.configOption}</code> を設定することで、SDK はユーザーがサインインした後に{' '}
  <a href="https://openid.net/specs/openid-connect-core-1_0.html#UserInfo">
    userinfo エンドポイント
  </a>{' '}
  にリクエストを送信してユーザー情報を取得し、リクエストが完了すると{' '}
  <code>{props.value ?? 'user.custom_data'}</code> が利用可能になります。
</>
)}
