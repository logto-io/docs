---
sidebar_label: Fiber
---

import AddValidationLogic from '../../../authorization/validate-access-tokens/go/_add-validation-logic.mdx';
import Init from '../../../authorization/validate-access-tokens/go/_init.mdx';
import ApplyMiddleware from '../../../authorization/validate-access-tokens/go/fragments/fiber/_apply-middleware.md';
import MainContent from '../../fragments/_main-content.mdx';

import InitProject from './_init-project.mdx';

# Fiber API をロールベースのアクセス制御 (RBAC) と JWT 検証で保護する

このガイドでは、Logto が発行する [ロールベースのアクセス制御 (RBAC)](/authorization/role-based-access-control) と [JSON Web Token (JWT)](https://auth.wiki/jwt) を使用して Fiber API をセキュアにするための認可 (Authorization) 実装方法を説明します。

{/* WARNING: We cannot use variables in the headings or introductory text since it will not be rendered correctly. */}

<MainContent
  framework="fiber"
  initializeProject={<InitProject />}
  languageInit={<Init />}
  applyMiddleware={<ApplyMiddleware />}
  addValidationLogic={<AddValidationLogic framework="fiber" />}
/>
