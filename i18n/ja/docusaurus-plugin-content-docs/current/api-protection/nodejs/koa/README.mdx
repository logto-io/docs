---
sidebar_label: Koa.js
---

import AddValidationLogic from '../../../authorization/validate-access-tokens/nodejs/_add-validation-logic.mdx';
import Init from '../../../authorization/validate-access-tokens/nodejs/_init.mdx';
import ApplyMiddleware from '../../../authorization/validate-access-tokens/nodejs/fragments/koa/_apply-middleware.md';
import MainContent from '../../fragments/_main-content.mdx';

import InitProject from './_init-project.mdx';

# RBAC と JWT 検証で Koa.js API を保護する

このガイドでは、Logto が発行する [ロールベースのアクセス制御 (RBAC)](/authorization/role-based-access-control) と [JSON Web Token (JWT)](https://auth.wiki/jwt) を利用して、Koa.js API に認可 (Authorization) を実装し、セキュリティを強化する方法を説明します。

{/* WARNING: We cannot use variables in the headings or introductory text since it will not be rendered correctly. */}

<MainContent
  framework="koa"
  initializeProject={<InitProject />}
  languageInit={<Init />}
  applyMiddleware={<ApplyMiddleware />}
  addValidationLogic={<AddValidationLogic framework="koa" />}
/>
