---
slug: /integrations/smsaero
sidebar_label: SMS Aero
sidebar_custom_props:
  description: SMS Aero ให้ผู้ใช้สามารถใช้บริการส่ง SMS ได้ภายใน 5 นาทีโดยไม่ต้องดูสัญญา นักพัฒนาจะได้รับ API ที่ใช้งานง่ายพร้อมคลาสที่เข้าถึงได้และการสนับสนุนผ่านแชทตลอด 24 ชั่วโมง
---

import GuideTip from '../../fragments/_guide-tip.mdx';

# ตั้งค่าการยืนยัน SMS ด้วย SMS Aero

ตัวเชื่อมต่อ Logto อย่างเป็นทางการสำหรับบริการข้อความสั้น SMS Aero

<GuideTip />

## ลงทะเบียนบัญชี \{#register-account}

สร้างบัญชีใหม่ที่ [SMSAero](https://smsaero.ru/) (ข้ามไปขั้นตอนถัดไปหากคุณมีบัญชีอยู่แล้ว)

## รับข้อมูลรับรองบัญชี \{#get-account-credentials}

เราจะต้องใช้ข้อมูลรับรอง API เพื่อให้ตัวเชื่อมต่อทำงาน ไปที่
[API และ SMPP](https://smsaero.ru/cabinet/settings/apikey/)

คัดลอก "API-key" หรือสร้างใหม่

## สร้าง JSON ของตัวเชื่อมต่อ \{#compose-the-connector-json}

กรอกช่อง _email_, _apiKey_ และ _senderName_ ด้วยอีเมล, API key และชื่อผู้ส่งของคุณ

คุณสามารถกรอกชื่อผู้ส่งเป็น "SMSAero" เพื่อใช้ชื่อผู้ส่งเริ่มต้นที่ SMSAero กำหนดไว้

คุณสามารถเพิ่มเทมเพลตตัวเชื่อมต่อ SMS หลายรายการสำหรับแต่ละกรณี ตัวอย่างเช่นการเพิ่มเทมเพลตเดียว:

- กรอกช่อง `content` ด้วยข้อความใด ๆ ที่เป็น string อย่าลืมเว้นที่ว่าง `{{code}}` สำหรับรหัสยืนยันแบบสุ่ม
- กรอกช่อง `usageType` ด้วยค่า `Register`, `SignIn`, `ForgotPassword`, `Generic` สำหรับแต่ละกรณีการใช้งาน
  เพื่อเปิดใช้งาน user flow แบบสมบูรณ์ จำเป็นต้องมีเทมเพลตที่มี usageType เป็น `Register`, `SignIn`, `ForgotPassword` และ `Generic`

### ทดสอบตัวเชื่อมต่อ SMSAero \{#test-smsaero-connector}

คุณสามารถกรอกหมายเลขโทรศัพท์และคลิก "Send" เพื่อตรวจสอบว่าการตั้งค่าสามารถใช้งานได้ก่อนกด "Save and Done"

เรียบร้อยแล้ว อย่าลืม <MainSiteUrl href="/connectors/sms-connectors/#enable-phone-number-sign-up-or-sign-in">เปิดใช้งานตัวเชื่อมต่อในประสบการณ์การลงชื่อเข้าใช้</MainSiteUrl>

### ประเภทของการตั้งค่า \{#config-types}

| ชื่อ       | ประเภท      |
| ---------- | ----------- |
| email      | string      |
| apiKey     | string      |
| senderName | string      |
| templates  | Templates[] |

| คุณสมบัติของเทมเพลต | ประเภท      | ค่าที่เป็นไปได้                                         |
| ------------------- | ----------- | ------------------------------------------------------- |
| content             | string      | N/A                                                     |
| usageType           | enum string | 'Register' \| 'SignIn' \| 'ForgotPassword' \| 'Generic' |

## อ้างอิง \{#reference}

- [เอกสาร API ของ SMSAero](https://smsaero.ru/integration/documentation/api/)
