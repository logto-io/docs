หากคุณยังไม่คุ้นเคยกับ องค์กร (Organization) โปรดอ่าน [🏢 องค์กร (หลายผู้เช่า; Multi-tenancy)](/organizations) เพื่อเริ่มต้น

คุณต้องเพิ่ม <code>{props.organizationScope}</code> ขอบเขต (scope) ขณะตั้งค่า Logto client:

{props.configOrganizationCode}

เมื่อผู้ใช้ลงชื่อเข้าใช้แล้ว คุณสามารถดึงโทเค็นองค์กร (organization token) สำหรับผู้ใช้ได้:

{props.getOrganizationAccessTokenCode}
