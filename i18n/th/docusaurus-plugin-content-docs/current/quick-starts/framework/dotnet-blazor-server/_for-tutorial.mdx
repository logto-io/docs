import Checkpoint from '../../fragments/_checkpoint-test-your-application.md';
import AddAuthentication from '../dotnet-mvc/_add-authentication.md';
import ConfigureRedirectUris from '../dotnet-mvc/_configure-redirect-uris.mdx';
import GuideTip from '../dotnet-mvc/_guide-tip.md';
import Installation from '../dotnet-mvc/_installation.md';
import SignInAndSignOutFlows from '../dotnet-mvc/_sign-in-and-sign-out-flows.mdx';

import AddRoutes from './_add-routes.md';
import AuthorizeViewComponent from './_authorize-view-component.md';
import ImplementButtons from './_implement-buttons.md';

<GuideTip />

### การติดตั้ง (Installation) \{#installation}

<Installation />

### เพิ่มการยืนยันตัวตน Logto (Add Logto authentication) \{#add-logto-authentication}

<AddAuthentication />

### กระบวนการลงชื่อเข้าใช้ / ออกจากระบบ (Sign-in and sign-out flows) \{#sign-in-and-sign-out-flows}

<SignInAndSignOutFlows />

### กำหนดค่า redirect URIs (Configure redirect URIs) \{#configure-redirect-uris}

<ConfigureRedirectUris />

### เพิ่มเส้นทาง (Add routes) \{#add-routes}

<AddRoutes />

### สร้างปุ่มลงชื่อเข้าใช้ / ออกจากระบบ (Implement sign-in / sign-out buttons) \{#implement-sign-insign-out-buttons}

<ImplementButtons />

### คอมโพเนนต์ `<AuthorizeView />` \{#the-authorizeview--component}

<AuthorizeViewComponent />

<Checkpoint />
