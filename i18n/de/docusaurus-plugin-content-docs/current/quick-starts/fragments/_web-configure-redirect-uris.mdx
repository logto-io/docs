{/* Dieses Fragment kombiniert die Konfiguration der Redirect-URI und der Redirect-URI nach dem Abmelden, was für bestimmte Frameworks hilfreich sein kann. */}

import redirectUriFigure from '@site/docs/quick-starts/assets/web-redirect-uri.png';

export const defaultBaseUrl = 'http://localhost:3000/';
export const defaultRedirectUri = `${defaultBaseUrl}callback`;

#### Redirect-URIs konfigurieren \{#configure-redirect-uris}

Wechsle zur Anwendungsdetailseite der Logto-Konsole. Füge eine Redirect-URI <code>{props.redirectUri ?? 'http://localhost:3000/callback'}</code> hinzu.

<img alt="Redirect-URI in der Logto-Konsole" src={redirectUriFigure} width="600px" />

Genau wie beim Anmelden sollten Benutzer zu Logto weitergeleitet werden, um sich von der gemeinsamen Sitzung abzumelden. Sobald dies abgeschlossen ist, wäre es ideal, den Benutzer zurück zu deiner Website zu leiten. Füge zum Beispiel <code>{defaultBaseUrl}</code> als Redirect-URI nach dem Abmelden hinzu.

Klicke dann auf "Speichern", um die Änderungen zu speichern.
