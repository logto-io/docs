Um ein neues Go-Projekt mit Chi zu initialisieren, kannst du folgende Schritte ausführen:

```bash
go mod init your-api-name
go get github.com/go-chi/chi/v5
```

Erstelle dann ein grundlegendes Chi-Server-Setup:

```go title="main.go"
package main

import (
    "net/http"

    "github.com/go-chi/chi/v5"
)

func main() {
    r := chi.NewRouter()

    http.ListenAndServe(":3000", r)
}
```

:::note
Weitere Informationen zur Einrichtung von Routen, Middleware und anderen Funktionen findest du in der Chi-Dokumentation.
:::
