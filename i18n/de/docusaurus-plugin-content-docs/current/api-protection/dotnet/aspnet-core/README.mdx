---
sidebar_label: ASP.NET Core
---

import AddValidationLogic from '../../../authorization/validate-access-tokens/dotnet/_add-validation-logic.mdx';
import ApplyMiddleware from '../../../authorization/validate-access-tokens/dotnet/_apply-middleware.mdx';
import Init from '../../../authorization/validate-access-tokens/dotnet/_init.mdx';
import MainContent from '../../fragments/_main-content.mdx';

import InitProject from './_init-project.mdx';

# Schütze deine ASP.NET Core API mit rollenbasierter Zugangskontrolle (RBAC) und JWT-Validierung

Diese Anleitung hilft dir, Autorisierung zu implementieren, um deine ASP.NET Core APIs mit [rollenbasierter Zugangskontrolle (RBAC)](/authorization/role-based-access-control) und [JSON Web Tokens (JWTs)](https://auth.wiki/jwt), die von Logto ausgestellt werden, abzusichern.

{/* WARNUNG: Wir können keine Variablen in Überschriften oder einleitenden Texten verwenden, da diese nicht korrekt gerendert werden. */}

<MainContent
  framework="aspnet-core"
  initializeProject={<InitProject />}
  languageInit={<Init />}
  applyMiddleware={<ApplyMiddleware />}
  addValidationLogic={<AddValidationLogic />}
/>
