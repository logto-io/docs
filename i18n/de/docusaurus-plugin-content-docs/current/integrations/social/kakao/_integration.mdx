## Ein Projekt in der Kakao Developers Console einrichten \{#set-up-a-project-in-the-kakao-devlopers-console}

- Besuche die [Kakao Developers Console](https://developers.kakao.com/console/app) und melde dich mit deinem Kakao-Konto an.
- Klicke auf **Eine Anwendung hinzufügen**, um ein neues Projekt zu erstellen, oder wähle ein bestehendes Projekt aus.

## Kakao Login konfigurieren \{#configure-kakao-login}

### Kakao Login aktivieren \{#activate-kakao-login}

- Klicke im Menü auf **Produkteinstellungen -> Kakao Login**.
- Schalte `Kakao Login Aktivierung` ein.
- Füge die folgende URL in `Redirect URI` ein:
  - `http(s)://YOUR_URL/callback/${connector_id}` (Die `connector_id` findest du in der oberen Leiste der Logto Admin Console auf der Seite mit den Connector-Details.)
  - (Bitte ersetze `YOUR_URL` mit deiner `Logto`-URL und wähle `http` oder `https` je nach deiner Situation.)

### Datenschutzeinstellung \{#privacy-setting}

- Klicke im Menü auf **Produkteinstellungen -> Kakao Login -> Zustimmungselement**.
- Ändere den Status von `Spitzname`, `Profilbild` und `E-Mail` zu **Erforderliche Zustimmung** (Möglicherweise kannst du `E-Mail` nicht auf **Erforderliche Zustimmung** ändern, aufgrund deiner Projekteinstellungen.)

### Sicherheitseinstellung (Optional) \{#security-setting-optional}

- Klicke im Menü auf **Produkteinstellungen -> Kakao Login -> Sicherheit**.
- Klicke auf den `Client-Geheimcode`, um einen Geheimcode zu generieren.
- Ändere den `Aktivierungsstatus` auf Aktivieren. (Wenn du es aktivierst, ist der `Geheimcode` erforderlich.)

## Logto konfigurieren \{#configure-logto}

### Konfigurationstypen \{#config-types}

| Name         | Typ     |
| ------------ | ------- |
| clientId     | string  |
| clientSecret | string? |

#### clientId \{#clientid}

`clientId` ist der `REST API-Schlüssel` deines Projekts.
(Du findest ihn in der `Zusammenfassung` deines Projekts in der Kakao Developers Console.)

#### clientSecret \{#clientseceret}

`clientSecret` ist der `Geheimcode` deines Projekts.
(Bitte überprüfe die [Sicherheitseinstellung (Optional)](#security-setting-optional))
