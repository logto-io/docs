---
sidebar_position: 2
---

import Columns from '@components/Columns';

# 创建并集成第一个应用

在「开始上手」标签页点击右侧的「创建按钮」，浏览器将重定向至「应用」标签页。接着点按页面中的「创建应用」按钮。

## 选择你的应用类型

在打开的对话框选择你想要集成的应用类型：

- **原生应用** 是运行在原生环境中的应用。例如 iOS 应用、Android 应用。
- **单页应用** 是运行在 web 浏览器中的应用，并且该应用无需重新载入整个页面，即可可以从服务器更新页面中的数据。例如 React DOM 应用、Vue 应用。
- **传统 web 应用** 是由 web 服务器全权负责渲染与更新页面的应用。例如 JSP、PHP。

对于每一种应用类型，我们准备了不同的教程。选择正确的应用类型将对后续流程有很大帮助。

## 输入应用名称

输入应用名称（例如「书店」）并点按「创建应用」。

## 集成 Logto

Ta-da! 你刚刚在 Logto 中成功创建了第一个应用。你将看到一个包含详细集成指南的恭喜页。跟着指南看看在你的应用体验是什么样的。

:::note
如果你不小心跳过了「管理面板」中的指南，你也可以查看下方列表中的指南，或者点按应用详情页中的「查看指南」按钮。
:::

### 集成指南

<Columns
  columns={[
    { title: 'Native App', items: [{ key: 'iOS' }, { key: 'Android' }] },
    { title: 'Single Page App', items: [{ key: 'Vanilla JS' }, { key: 'React' }, { key: 'Vue' }] },
    { title: 'Tranditional Web App', items: [{ key: 'ExpressJS' }, { key: 'Other Web Servers' }] },
  ]}
/>

## 接下来

让我们通过「管理面板」给「登录体验」添加一些品牌元素。
