你需要一个满足如下条件的 Access Token：

<ul>
  <li>是一个有效的 token。</li>
  <li>颁发自你的 Logto 实例。</li>
  <li>
    它的 audience（<code>aud</code>，即 API indentifier）指向 <code>https://api.logto.io</code>。
  </li>
  {props.children && <li>{props.children}</li>}
</ul>
