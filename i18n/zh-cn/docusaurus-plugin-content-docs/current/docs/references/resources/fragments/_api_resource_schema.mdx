<table>
  <thead>
    <tr>
      <td>
        <strong>名称</strong>
      </td>
      <td>
        <strong>描述</strong>
      </td>
      <td>
        <strong>必填项</strong>
      </td>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>API Name</td>
      <td>一个可读性高的 API 接口名称，用于帮助你识别该接口。</td>
      <td>是</td>
    </tr>
    <tr>
      <td>API Identifier</td>
      <td>
        一个全局唯一的{' '}
        <a href="https://www.rfc-editor.org/rfc/rfc8707.html">API Resource Indicator</a>{' '}
        配置。（详见上述的 Resource Indicator 定义）。
        <br />
        Logto 将会使用这个参数作为唯一标识来识别被请求资源并颁发对应的识别令牌。
      </td>
      <td>是</td>
    </tr>
    <tr>
      <td>
        令牌过期时间 <i>（秒）</i>
      </td>
      <td>
        所颁发 <strong>access_token</strong> 的有效时长。默认的有效时长为 <strong>3600</strong>。
      </td>
      <td>否</td>
    </tr>
  </tbody>
</table>

:::caution
一个带有过期令牌的请求将被视为无效请求。
这个设置是为了防止你的接口长期暴露在一个可能被劫持的令牌下。
该数值越高，一个令牌的有效生命周期就越长，你的接口就将面临在更大的风险。
:::
