<p>
  让我们在这里切换到「管理控制台」的应用详情页。添加一个值为 {}
  <code>{props.postSignOutRedirectUri}</code> 的 Post Sign-out Redirect URI 并点按「保存变更」。
</p>

<img alt="「管理控制台」中的 Post Sign-out Redirect URI" src={props.figureSrc} width="600px" />

[Post Sign-outRedirect URI](https://openid.net/specs/openid-connect-rpinitiated-1_0.html#RPLogout) 是一个 OAuth 2.0 的概念，用于表明在用户退出登录后重定向至的位置。
