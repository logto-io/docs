<p>
  让我们在这里切换到「管理控制台」的应用详情页。添加一个值为 {}
  <code>{props.redirectUri}</code> 的 Redirect URI 并点按「保存变更」。
</p>

<img alt="「管理控制台」中的 Redirect URI" src={props.figureSrc} width="600px" />

[Redirect URI](https://www.oauth.com/oauth2-servers/redirect-uris/) 是一个 OAuth 2.0 的概念，用于表明在 authentication 后重定向至的位置。
