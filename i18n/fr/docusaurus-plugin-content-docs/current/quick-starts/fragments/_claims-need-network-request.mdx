{props.type ? null : (() => {throw new Error(`props.type is required when importing _claims-need-network-request.mdx`)})()}

### Revendications nécessitant des requêtes réseau \{#claims-that-need-network-requests}

Pour éviter de surcharger le jeton d’identifiant (ID token), certaines revendications nécessitent des requêtes réseau pour être récupérées. Par exemple, la revendication `custom_data` n'est pas incluse dans l'objet utilisateur même si elle est demandée dans les portées. Pour accéder à ces revendications, <span>{props.type === 'method' && (<>vous pouvez utiliser la méthode <code>{props.method}</code></>)}{props.type === 'option' && <>vous pouvez configurer l'option <code>{props.configOption}</code></>}</span> :

{props.codeSnippet}

{props.type === 'method' && (

<>
  Cette méthode récupérera les informations de l'utilisateur en faisant une requête à l'{' '}
  <a href="https://openid.net/specs/openid-connect-core-1_0.html#UserInfo">
    point de terminaison userinfo
  </a>
  . Pour en savoir plus sur les portées et revendications disponibles, consultez la section{' '}
  <a href="#scopes-and-claims">Portées et revendications</a>.
</>
)}

{props.type === 'option' && (

<>
  En configurant <code>{props.configOption}</code>, le SDK récupérera les informations de
  l'utilisateur en faisant une requête à l'{' '}
  <a href="https://openid.net/specs/openid-connect-core-1_0.html#UserInfo">
    point de terminaison userinfo
  </a>{' '}
  après que l'utilisateur se soit connecté, et <code>{props.value ?? 'user.custom_data'}</code> sera
  disponible une fois la requête terminée.
</>
)}
