---
slug: /quick-starts/expo
sidebar_label: Expo (React Native)
sidebar_custom_props:
  darkLogoFilename: 'expo-dark.svg'
  description: Créez des applications natives universelles avec React qui fonctionnent sur Android, iOS et le web.
language: js
official_link: https://expo.dev
app_type: Native app
framework: Expo
tutorial_name: Expo (React Native)
---

import FurtherReadings from '../../fragments/_further-readings.md';

import GuideTip from './_guide-tip.md';
import Installation from './_installation.mdx';
import Integration from './_integration.mdx';
import ApiResources from './api-resources/_index.mdx';
import GetUserInformation from './get-user-information/_index.mdx';

# Ajoutez l’authentification à votre application Expo (React Native)

<GuideTip />

## Prérequis \{#prerequisites}

- Un compte [Logto Cloud](https://cloud.logto.io) ou un [Logto auto-hébergé](/introduction/set-up-logto-oss).
- Une application native Logto créée.

## Installation \{#installation}

<Installation />

## Intégration \{#integration}

<Integration />

## Obtenez des informations sur l'utilisateur \{#get-user-information}

<GetUserInformation />

## Ressources API et organisations \{#api-resources-and-organizations}

<ApiResources />

## Dépannage \{#troubleshooting}

### Impossible de résoudre `@logto/client/shim` \{#unable-to-resolve-logtoclientshim}

Pour les projets Expo, si vous rencontrez l'erreur `Unable to resolve "@logto/client/shim" from "node_modules/@logto/rn/lib/index.js"`, vous pouvez la résoudre en ajoutant ce qui suit à votre fichier `metro.config.js` :

```js title="metro.config.js"
const config = {
  // ...
  resolver: {
    unstable_enablePackageExports: true,
  },
};

module.exports = config;
```

Cette erreur indique que le package `@logto/rn` n'est pas capable de résoudre le module `@logto/client/shim`.

Comme les exports de nœuds ont été utilisés dans le package `@logto/client`, et que les exports de package ne sont pas activés par défaut dans le bundler Metro, vous devez les activer manuellement.

Voir le [support des exports de package React Native](https://reactnative.dev/blog/2023/06/21/package-exports-support#enabling-package-exports-beta) pour plus de détails.

## Lectures complémentaires \{#further-readings}

<FurtherReadings />
