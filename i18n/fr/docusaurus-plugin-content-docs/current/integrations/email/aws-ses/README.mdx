---
slug: /integrations/aws-ses
sidebar_label: AWS Direct Mail
sidebar_custom_props:
  description: Amazon SES est un fournisseur de services de messagerie cloud qui peut s'intégrer à toute application pour l'envoi de courriels en masse.
  logoFilename: 'aws.svg'
---

import GuideTip from '../../fragments/_guide-tip.mdx';

# Configurer la vérification par e-mail avec AWS Direct Mail

Le connecteur officiel Logto pour le connecteur AWS pour le service de messagerie directe.

<GuideTip />

## Commencer

Amazon SES est un fournisseur de services de messagerie cloud qui peut s'intégrer à toute application pour l'envoi de courriels en masse.

L'équipe Logto appelle les APIs Amazon Simple Email Service, grâce auxquelles les utilisateurs finaux de Logto peuvent s'inscrire et se connecter à leur compte Logto via un code de vérification par e-mail.

## Configurer un service de messagerie dans la console de service AWS

> 💡 **Astuce**
>
> Vous pouvez ignorer certaines sections si vous avez déjà terminé.

### Enregistrer un compte AWS

Allez sur [AWS](https://aws.amazon.com/) et enregistrez un compte.

### Créer une identité

- Allez sur la console `Amazon Simple Email Service`
- Créez une identité, choisissez l'une des options suivantes
  - Créer un domaine
  - Créer une adresse e-mail

### Configuration du connecteur

1. Cliquez sur votre nom d'utilisateur dans le coin supérieur droit de la console Amazon pour entrer dans `Security Credentials`. Si vous n'en avez pas, créez un `AccessKey` et enregistrez-le soigneusement.
2. Complétez les paramètres du connecteur `Amazon Simple Email Service` :
   - Utilisez l'`AccessKey ID` et l'`AccessKey Secret` obtenus à l'étape 1 pour remplir respectivement `accessKeyId` et `accessKeySecret`.
   - `region` : Remplissez le champ `region` avec la région de l'identité que vous utilisez pour envoyer des courriels.
   - `emailAddress` : L'adresse e-mail que vous utilisez pour envoyer des courriels, au format `Logto\<noreply@logto.io>` ou `\<noreply@logto.io>`

les paramètres suivants sont facultatifs ; la description des paramètres peut être trouvée dans la [documentation de l'API AWS SES](https://docs.aws.amazon.com/ses/latest/APIReference-V2/API_SendEmail.html).

- `feedbackForwardingEmailAddress`
- `feedbackForwardingEmailAddressIdentityArn`
- `configurationSetName`

### Tester le connecteur Amazon SES

Vous pouvez taper une adresse e-mail et cliquer sur "Envoyer" pour voir si les paramètres fonctionnent avant "Enregistrer et Terminer".

C'est tout. N'oubliez pas de [Activer le connecteur dans l'expérience de connexion](/connectors/email-connectors/#enable-email-sign-up-or-sign-in).

### Configurer les types

| Nom                                       | Type              |
| ----------------------------------------- | ----------------- |
| accessKeyId                               | string            |
| accessKeySecret                           | string            |
| region                                    | string            |
| emailAddress                              | string (OPTIONAL) |
| emailAddressIdentityArn                   | string (OPTIONAL) |
| feedbackForwardingEmailAddress            | string (OPTIONAL) |
| feedbackForwardingEmailAddressIdentityArn | string (OPTIONAL) |
| configurationSetName                      | string (OPTIONAL) |
| templates                                 | Template[]        |

| Propriétés du modèle | Type        | Valeurs énumérées                                       |
| -------------------- | ----------- | ------------------------------------------------------- |
| subject              | string      | N/A                                                     |
| content              | string      | N/A                                                     |
| usageType            | enum string | 'Register' \| 'SignIn' \| 'ForgotPassword' \| 'Generic' |
