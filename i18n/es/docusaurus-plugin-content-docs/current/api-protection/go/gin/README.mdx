---
sidebar_label: Gin
---

import AddValidationLogic from '../../../authorization/validate-access-tokens/go/_add-validation-logic.mdx';
import Init from '../../../authorization/validate-access-tokens/go/_init.mdx';
import ApplyMiddleware from '../../../authorization/validate-access-tokens/go/fragments/gin/_apply-middleware.md';
import MainContent from '../../fragments/_main-content.mdx';

import InitProject from './_init-project.mdx';

# Protege tu API de Gin con control de acceso basado en roles (RBAC) y validación de JWT

Esta guía te ayudará a implementar autorización para asegurar tus APIs de Gin utilizando el [control de acceso basado en roles (RBAC)](/authorization/role-based-access-control) y [JSON Web Tokens (JWTs)](https://auth.wiki/jwt) emitidos por Logto.

{/* ADVERTENCIA: No podemos usar variables en los encabezados o el texto introductorio ya que no se renderizarán correctamente. */}

<MainContent
  framework="gin"
  initializeProject={<InitProject />}
  languageInit={<Init />}
  applyMiddleware={<ApplyMiddleware />}
  addValidationLogic={<AddValidationLogic framework="gin" />}
/>
