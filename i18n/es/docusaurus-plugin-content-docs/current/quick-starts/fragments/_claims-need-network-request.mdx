{props.type ? null : (() => {throw new Error(`props.type is required when importing _claims-need-network-request.mdx`)})()}

### Reclamos que necesitan solicitudes de red

Para evitar sobrecargar el Token de ID, algunos reclamos requieren solicitudes de red para ser obtenidos. Por ejemplo, el reclamo `custom_data` no se incluye en el objeto de usuario incluso si se solicita en los alcances. Para acceder a estos reclamos, <span>{props.type === 'method' && (<>puedes usar el método <code>{props.method}</code></>)}{props.type === 'option' && <>puedes configurar la opción <code>{props.configOption}</code></>}</span>:

{props.codeSnippet}

{props.type === 'method' && (

<>
  Este método obtendrá la información del usuario solicitando al{' '}
  <a href="https://openid.net/specs/openid-connect-core-1_0.html#UserInfo">endpoint de userinfo</a>.
  Para aprender más sobre los alcances y reclamos disponibles, consulta la sección de{' '}
  <a href="#scopes-and-claims">Alcances y reclamos</a>.
</>
)}

{props.type === 'option' && (

<>
  Al configurar <code>{props.configOption}</code>, el SDK obtendrá la información del usuario
  solicitando al{' '}
  <a href="https://openid.net/specs/openid-connect-core-1_0.html#UserInfo">endpoint de userinfo</a>{' '}
  después de que el usuario haya iniciado sesión, y <code>{props.value ?? 'user.custom_data'}</code>{' '}
  estará disponible una vez que la solicitud se complete.
</>
)}
