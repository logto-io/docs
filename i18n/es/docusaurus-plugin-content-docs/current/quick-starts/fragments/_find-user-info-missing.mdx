Es posible que encuentres que falta alguna información del usuario en el objeto devuelto desde <code>{props.method}</code>. Esto se debe a que OAuth 2.0 y OpenID Connect (OIDC) están diseñados para seguir el principio de privilegio mínimo (PoLP), y Logto está construido sobre estos estándares.
