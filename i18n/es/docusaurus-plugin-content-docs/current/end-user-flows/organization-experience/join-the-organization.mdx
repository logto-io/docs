---
sidebar_position: 7
---

# Unirse a la organización

## Dónde se utiliza \{#where-to-use-it}

La lista de organizaciones suele aparecer durante el flujo de incorporación de usuarios. Por ejemplo, cuando un administrador te invita a un espacio de trabajo.

Desde la perspectiva del producto, puede aparecer en dos lugares principales:

- El **buscador de organizaciones** durante el inicio de sesión o registro

<img
  src="/img/assets/join-organization-on-sign-in-or-sign-up.png"
  alt="Unirse a la organización durante el inicio de sesión o registro"
/>

- El **cambiador de organizaciones** dentro de la aplicación

<img
  src="/img/assets/join-organization-in-app.png"
  alt="Unirse a la organización dentro de la aplicación"
/>

## Usa la Management API de Logto para permitir que los usuarios se unan a una organización \{#use-the-logto-management-api-to-let-users-join-an-organization}

En la sección anterior, cubrimos cómo crear y enviar invitaciones a organizaciones usando la Management API de Logto.
A continuación, implementa una página de destino que maneje el enlace de invitación cuando los invitados lleguen a tu aplicación.

- `GET /api/organization-invitations` y `GET /api/organization-invitations/{id}`: Obtén todas las invitaciones o una específica por ID.
  En tu página de destino, utiliza estas APIs para listar todas las invitaciones o mostrar los detalles de una invitación específica que el usuario haya recibido.
- `PUT /api/organization-invitations/{id}/status`: Acepta o rechaza la invitación actualizando su estado.
  Utiliza esta API para manejar la respuesta del usuario.
