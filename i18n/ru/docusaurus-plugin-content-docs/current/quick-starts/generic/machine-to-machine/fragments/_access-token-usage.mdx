Вы можете заметить, что в ответе токена есть поле `token_type`, которое фиксировано как `Bearer`.

Таким образом, вы должны поместить токен доступа в поле `Authorization` HTTP-заголовков в формате Bearer (`Bearer YOUR_TOKEN`), когда взаимодействуете с вашим сервером ресурса API.
