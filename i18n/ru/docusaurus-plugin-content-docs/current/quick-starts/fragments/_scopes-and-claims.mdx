import ScopeClaimList from './_scope-claim-list.md';

Logto использует [конвенции областей действия и утверждений](https://openid.net/specs/openid-connect-core-1_0.html#Claims) OIDC для определения областей действия и утверждений для получения информации о пользователе из ID токена и OIDC <a href="https://openid.net/specs/openid-connect-core-1_0.html#UserInfo">userinfo endpoint</a>. Оба термина "область действия" и "утверждение" происходят из спецификаций OAuth 2.0 и OpenID Connect (OIDC).

{/* TODO: Remove below */}

{props.configScopesCode &&

<>
<p>
Вкратце, когда вы запрашиваете область действия, вы получаете соответствующие утверждения в информации о пользователе. Например, если вы запрашиваете область действия `email`, вы получите данные `email` и `email_verified` пользователя.
</p>

<p>
  По умолчанию, Logto SDK всегда запрашивает три области действия: `openid`, `profile` и
  `offline_access`, и нет возможности удалить эти области действия по умолчанию. Но вы можете
  добавить больше областей действия при настройке Logto:
</p>

{props.configScopesCode}

</>
}

<ScopeClaimList />
