---
sidebar_label: Express.js
---

import AddValidationLogic from '../../../authorization/validate-access-tokens/nodejs/_add-validation-logic.mdx';
import Init from '../../../authorization/validate-access-tokens/nodejs/_init.mdx';
import ApplyMiddleware from '../../../authorization/validate-access-tokens/nodejs/fragments/express/_apply-middleware.md';
import MainContent from '../../fragments/_main-content.mdx';

import InitProject from './_init-project.mdx';

# Proteja sua API Express.js com RBAC (controle de acesso baseado em papel) e validação de JWT

Este guia irá ajudá-lo a implementar autorização para proteger suas APIs Express.js usando [controle de acesso baseado em papel (RBAC)](/authorization/role-based-access-control) e [JSON Web Tokens (JWTs)](https://auth.wiki/jwt) emitidos pelo Logto.

{/* AVISO: Não podemos usar variáveis nos títulos ou no texto introdutório, pois não serão renderizadas corretamente. */}

<MainContent
  framework="express"
  initializeProject={<InitProject />}
  languageInit={<Init />}
  applyMiddleware={<ApplyMiddleware />}
  addValidationLogic={<AddValidationLogic framework="express" />}
/>
