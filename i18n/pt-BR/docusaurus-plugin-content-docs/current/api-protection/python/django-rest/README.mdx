---
sidebar_label: Django REST Framework
---

import AddValidationLogic from '../../../authorization/validate-access-tokens/python/_add-validation-logic.mdx';
import Init from '../../../authorization/validate-access-tokens/python/_init.mdx';
import ApplyMiddleware from '../../../authorization/validate-access-tokens/python/fragments/django-rest/_apply-middleware.md';
import MainContent from '../../fragments/_main-content.mdx';

import InitProject from './_init-project.mdx';

# Proteja sua API Django REST Framework com RBAC e validação de JWT

Este guia irá ajudá-lo a implementar autorização para proteger suas APIs Django REST Framework usando [controle de acesso baseado em papel (RBAC)](/authorization/role-based-access-control) e [JSON Web Tokens (JWTs)](https://auth.wiki/jwt) emitidos pelo Logto.

{/* AVISO: Não podemos usar variáveis nos títulos ou no texto introdutório, pois não serão renderizadas corretamente. */}

<MainContent
  framework="django-rest"
  initializeProject={<InitProject />}
  languageInit={<Init />}
  applyMiddleware={<ApplyMiddleware />}
  addValidationLogic={<AddValidationLogic framework="django-rest" />}
/>
