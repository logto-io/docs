---
sidebar_label: ASP.NET Core
---

import AddValidationLogic from '../../../authorization/validate-access-tokens/dotnet/_add-validation-logic.mdx';
import ApplyMiddleware from '../../../authorization/validate-access-tokens/dotnet/_apply-middleware.mdx';
import Init from '../../../authorization/validate-access-tokens/dotnet/_init.mdx';
import MainContent from '../../fragments/_main-content.mdx';

import InitProject from './_init-project.mdx';

# Proteja sua API ASP.NET Core com RBAC (controle de acesso baseado em papel) e validação de JWT

Este guia irá ajudá-lo a implementar autorização para proteger suas APIs ASP.NET Core usando [controle de acesso baseado em papel (RBAC)](/authorization/role-based-access-control) e [JSON Web Tokens (JWTs)](https://auth.wiki/jwt) emitidos pelo Logto.

{/* AVISO: Não podemos usar variáveis nos títulos ou no texto introdutório, pois não serão renderizadas corretamente. */}

<MainContent
  framework="aspnet-core"
  initializeProject={<InitProject />}
  languageInit={<Init />}
  applyMiddleware={<ApplyMiddleware />}
  addValidationLogic={<AddValidationLogic />}
/>
