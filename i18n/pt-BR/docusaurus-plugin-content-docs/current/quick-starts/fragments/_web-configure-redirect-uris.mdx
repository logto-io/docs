{/* Este fragmento combina a configuração de URI de redirecionamento e URI de redirecionamento pós logout, o que pode ser útil para certos frameworks. */}

import redirectUriFigure from '../assets/web-redirect-uri.png';

export const defaultBaseUrl = 'http://localhost:3000/';
export const defaultRedirectUri = `${defaultBaseUrl}callback`;

#### Configurar URIs de redirecionamento \{#configure-redirect-uris}

Vá para a página de detalhes do aplicativo no Logto Console. Adicione um URI de redirecionamento <code>{props.redirectUri ?? 'http://localhost:3000/callback'}</code>.

<img alt="URI de redirecionamento no Logto Console" src={redirectUriFigure} width="600px" />

Assim como no login, os usuários devem ser redirecionados para o Logto para sair da sessão compartilhada. Uma vez concluído, seria ótimo redirecionar o usuário de volta para o seu site. Por exemplo, adicione <code>{defaultBaseUrl}</code> como a seção de URI de redirecionamento pós logout.

Em seguida, clique em "Salvar" para salvar as alterações.
