---
sidebar_position: 1
---

# 커넥터 파일 구조

첨부된 파일 트리는 커넥터의 파일이 TypeScript 프로젝트로 어떻게 구성되어 있는지를 보여줍니다.

파일 이름이 `^`로 끝나는 파일은 선택적 파일을 나타내며, 그렇지 않은 경우 필수 파일입니다.

```
ConnectorRootDir/
├── package.json
├── README.md
├── logo.svg
├── logo-dark.svg^
└── src/
    ├── index.ts
    ├── index.test.ts
    ├── types.ts
    ├── constant.ts
    ├── mock.ts^
    ├── utils.ts^
    ├── utils.test.ts^
    └── ...
```

커넥터 파일 구조를 기반으로 각 파일을 살펴보고 전체 시스템으로서 어떻게 작동하는지 알아봅시다.

### package.json \{#packagejson}

`package.json`은 이 커넥터 저장소의 구성을 포함합니다. 이는 공통 종속성과 속성을 포함하는 공유 `package.json`과 병합됩니다.

### README.md \{#readmemd}

`README.md`는 커넥터의 설정을 위한 자세한 가이드입니다.

### logo.svg \{#logosvg}

`logo.svg`는 벡터 이미지 형식으로 커넥터의 로고를 저장하는 파일입니다.

### logo-dark.svg \{#logo-darksvg}

`logo-dark.svg` 파일은 커넥터의 다크 모드 로고의 벡터 그래픽을 포함합니다.

:::note
`logo.svg`와 `logo-dark.svg` 간의 관계에 대해 더 알고 싶다면 [커넥터 로고](/connectors/connector-data-structure/#logo)를 참조하세요.
:::

### index.ts \{#indexts}

`index.ts`는 커넥터 클래스 구현을 위한 파일입니다.

### index.test.ts \{#indextestts}

`index.test.ts`는 `index.ts`의 구현에 대한 해당 단위 테스트 (UT)를 포함하는 파일입니다.

### types.ts \{#typests}

`types.ts`는 변수의 타입을 정의해야 하는 곳입니다.

### constant.ts \{#constantts}

`constant.ts`는 엔드포인트, 커넥터 메타데이터 등을 포함하여 커넥터와 관련된 상수를 넣어야 하는 곳입니다.

### mock.ts \{#mockts}

`mock.ts`는 테스트 목적으로 모의 값을 넣을 수 있는 파일입니다.

### utils.ts \{#utilsts}

`utils.ts`는 개발자가 유틸리티 함수를 넣는 파일이며, `utils.test.ts`는 관련 UT를 포함해야 합니다.
