import CodeBlock from '@theme/CodeBlock';

import AssumingUrl, { defaultBaseUrl } from '../../fragments/_web-assuming-url.mdx';

<AssumingUrl />

먼저, **Logto 리디렉션 URI**를 구성해 봅시다. 다음 URI를 Logto 애플리케이션 세부 정보 페이지의 "리디렉션 URI" 목록에 추가하세요:

<CodeBlock>{`http://${defaultBaseUrl}/Callback`}</CodeBlock>

**Logto 로그아웃 후 리디렉션 URI**를 구성하려면, 다음 URI를 Logto 애플리케이션 세부 정보 페이지의 "로그아웃 후 리디렉션 URI" 목록에 추가하세요:

<CodeBlock>{`http://${defaultBaseUrl}/SignedOutCallback`}</CodeBlock>

#### 기본 경로 변경 \{#change-the-default-paths}

**Logto 리디렉션 URI**는 기본 경로로 `/Callback`을 가지며, **Logto 로그아웃 후 리디렉션 URI**는 기본 경로로 `/SignedOutCallback`을 가집니다.

특별한 요구 사항이 없다면 그대로 두어도 됩니다. 변경하고 싶다면, `LogtoOptions`의 `CallbackPath`와 `SignedOutCallbackPath` 속성을 설정할 수 있습니다:

```csharp title="Program.cs"
builder.Services.AddLogtoAuthentication(options =>
{
  // 다른 설정...
  // highlight-start
  options.CallbackPath = "/Foo";
  options.SignedOutCallbackPath = "/Bar";
  // highlight-end
});
```

Logto 애플리케이션 세부 정보 페이지의 값을 이에 맞게 업데이트하는 것을 잊지 마세요.
