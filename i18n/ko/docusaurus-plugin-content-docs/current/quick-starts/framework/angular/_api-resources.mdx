import ApiResourcesDescription from '../../fragments/_api-resources-description.md';
import ConfigApiResources from '../../fragments/_config-api-resources.mdx';

import ConfigResourcesCode from './code/_config-resources-code.md';
import ConfigResourcesWithScopesCode from './code/_config-resources-with-scopes-code.md';

### API 리소스를 위한 `angular-auth-oidc-client` 구성하기 \{#configure-angular-auth-oidc-client-for-api-resource}

<ApiResourcesDescription />

<ConfigApiResources
  configResourcesCode={<ConfigResourcesCode />}
  configResourcesWithScopesCode={<ConfigResourcesWithScopesCode />}
/>

이제 액세스 토큰 (Access token)은 불투명 토큰 (Opaque token) 대신 JSON Web Token (JWT) 형식으로 제공됩니다.

:::warning
`resource`가 설정되면 `autoUserInfo`와 `renewUserInfoAfterTokenRenew`는 비활성화됩니다. 이는 액세스 토큰 (Access token)이 사용자 정보 엔드포인트가 아닌 특정 API 리소스를 위해 요청되기 때문입니다.
:::

현재, Logto 공식 SDK만이 사용자 정보와 API 리소스 액세스 토큰 (Access tokens)을 모두 요청할 수 있는 기능을 지원합니다. 둘 다 요청해야 하는 경우, 주저하지 말고 저희에게 연락해 주세요.
