조직이 처음이라면, 시작하기 위해 [🏢 조직 (다중 테넌시)](/organizations)을 읽어보세요.

Logto 클라이언트를 구성할 때 <code>{props.organizationScope}</code> 스코프를 추가해야 합니다:

{props.configOrganizationCode}

사용자가 로그인하면, 사용자를 위한 조직 토큰을 가져올 수 있습니다:

{props.getOrganizationAccessTokenCode}
