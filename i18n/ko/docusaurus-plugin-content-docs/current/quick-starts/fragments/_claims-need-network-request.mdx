{props.type ? null : (() => {throw new Error(`props.type is required when importing _claims-need-network-request.mdx`)})()}

### 네트워크 요청이 필요한 클레임 \{#claims-that-need-network-requests}

ID 토큰의 비대화를 방지하기 위해, 일부 클레임은 네트워크 요청을 통해 가져와야 합니다. 예를 들어, `custom_data` 클레임은 스코프에서 요청되더라도 사용자 객체에 포함되지 않습니다. 이러한 클레임에 접근하려면, <span>{props.type === 'method' && (<> <code>{props.method}</code> 메서드를 사용할 수 있습니다</>)}{props.type === 'option' && <> <code>{props.configOption}</code> 옵션을 구성할 수 있습니다</>}</span>:

{props.codeSnippet}

{props.type === 'method' && (

<>
  이 메서드는{' '}
  <a href="https://openid.net/specs/openid-connect-core-1_0.html#UserInfo">userinfo 엔드포인트</a>에
  요청하여 사용자 정보를 가져옵니다. 사용 가능한 스코프와 클레임에 대해 더 알고 싶다면,{' '}
  <a href="#scopes-and-claims">스코프와 클레임</a> 섹션을 참조하세요.
</>
)}

{props.type === 'option' && (

<>
  <code>{props.configOption}</code>을 구성하면, SDK는 사용자가 로그인한 후{' '}
  <a href="https://openid.net/specs/openid-connect-core-1_0.html#UserInfo">userinfo 엔드포인트</a>에
  요청하여 사용자 정보를 가져오며, 요청이 완료되면 <code>{props.value ?? 'user.custom_data'}</code>
  를 사용할 수 있게 됩니다.
</>
)}
