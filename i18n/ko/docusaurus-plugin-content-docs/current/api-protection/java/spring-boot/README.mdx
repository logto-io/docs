---
sidebar_label: Spring Boot
---

import AddValidationLogic from '../../../authorization/validate-access-tokens/java/_add-validation-logic.mdx';
import Init from '../../../authorization/validate-access-tokens/java/_init.mdx';
import ApplyMiddleware from '../../../authorization/validate-access-tokens/java/fragments/spring-boot/_apply-middleware.md';
import MainContent from '../../fragments/_main-content.mdx';

import InitProject from './_init-project.mdx';

# RBAC 및 JWT 검증으로 Spring Boot API 보호하기

이 가이드는 [역할 기반 접근 제어 (RBAC)](/authorization/role-based-access-control)와 Logto에서 발급된 [JSON Web Token (JWT)](https://auth.wiki/jwt)을 사용하여 Spring Boot API를 안전하게 보호하는 인가 (Authorization) 구현 방법을 안내합니다.

{/* 경고: 제목이나 소개 텍스트에는 변수를 사용할 수 없습니다. 올바르게 렌더링되지 않습니다. */}

<MainContent
  framework="spring-boot"
  initializeProject={<InitProject />}
  languageInit={<Init />}
  applyMiddleware={<ApplyMiddleware />}
  addValidationLogic={<AddValidationLogic framework="spring-boot" />}
/>
