새로운 .NET Web API 프로젝트를 초기화하려면 .NET CLI를 사용할 수 있습니다:

```bash
dotnet new webapi -n YourApiName
cd YourApiName
```

JWT 인증 (Authentication)을 위한 필수 NuGet 패키지를 추가하세요:

```bash
dotnet add package Microsoft.AspNetCore.Authentication.JwtBearer
```

기본 API 컨트롤러를 생성하세요:

```csharp title="Controllers/ApiController.cs"
using Microsoft.AspNetCore.Mvc;

namespace YourApiName.Controllers
{
    [ApiController]
    [Route("api/[controller]")]
    public class ApiController : ControllerBase
    {
        [HttpGet]
        public IActionResult Get()
        {
            return Ok(new { message = "Hello from .NET API" });
        }
    }
}
```

개발 서버를 시작하세요:

```bash
dotnet run
```

:::note
컨트롤러, 미들웨어 및 기타 기능 설정에 대한 자세한 내용은 ASP.NET Core 문서를 참조하세요.
:::
