---
slug: /integrations/aliyun-dm
sidebar_label: 阿里雲 Direct Mail
sidebar_custom_props:
  description: 阿里雲為線上企業提供雲端運算服務。
  logoFilename: 'aliyun.svg'
---

import GuideTip from '../../fragments/_guide-tip.mdx';

# 使用阿里雲 Direct Mail 設定電子郵件驗證

Logto 官方提供的阿里雲 Direct Mail 服務連接器。

<GuideTip />

## 開始使用 \{#get-started}

阿里雲是亞洲主要的雲端服務供應商，提供多種雲端服務，包括 DM（Direct Mail）。阿里雲 DM 連接器是由 Logto 團隊提供的外掛，用於呼叫阿里雲 DM 服務 API，協助 Logto 終端使用者透過郵件驗證碼（或稱驗證碼）註冊並登入 Logto 帳號。

## 在阿里雲 DirectMail 控制台設定郵件服務 \{#set-up-an-email-service-in-aliyun-directmail-console}

### 建立阿里雲帳號 \{#create-an-aliyun-account}

前往 [阿里雲](https://aliyun.com/)，若尚未註冊請建立你的阿里雲帳號。

### 啟用並設定阿里雲 Direct Mail \{#enable-and-configure-aliyun-direct-mail}

進入 [DM 服務控制台頁面](https://www.aliyun.com/product/directmail) 並登入。點擊頁面左上角的「申請開通」按鈕以啟用 Direct Mail 服務，並開始設定流程。

從 [DM 管理控制台頁面](https://dm.console.aliyun.com/) 開始，請依下列步驟操作：

1. 從側邊欄進入「發信域名」，依照指示新增「新建域名」。
2. 分別自訂「發信地址」與「郵件標籤」。

完成設定後，有兩種方式可進行測試：

- 前往 [DirectMail 概覽頁](https://dm.console.aliyun.com/)，在頁面底部找到「操作引導」，點擊「發送郵件」，即可看到各種測試方式。
- 依側邊欄路徑「發送郵件」->「發送郵件」建立測試任務。

## 編寫連接器 JSON \{#compose-the-connector-json}

1. 在 [DM 管理控制台頁面](https://dm.console.aliyun.com/) 右上角將滑鼠移至頭像，進入「AccessKey 管理」，點擊「創建 AccessKey」。完成安全驗證後，你會取得一組「AccessKey ID」與「AccessKey Secret」，請妥善保存。
2. 回到剛才造訪的「發信地址」或「郵件標籤」分頁，即可找到 _發信地址_ 或 _郵件標籤_。
3. 填寫阿里雲 DM 連接器設定：
   - 將步驟 1 取得的 access key 對應填入 `accessKeyId` 與 `accessKeySecret` 欄位。
   - 將步驟 2 找到的「發信地址」與「郵件標籤」分別填入 `accountName` 與 `fromAlias` 欄位。所有範本將共用此簽名名稱。（`fromAlias` 可留空，為選填。）
   - 你可以針對不同情境新增多個 DM 連接器範本。以下為新增單一範本的範例：
     - 填寫 `subject` 欄位，作為寄送郵件的標題。
     - `content` 欄位填入任意字串內容，別忘了保留 `{{code}}` 佔位符以插入隨機驗證碼。
     - `usageType` 欄位請依不同用途填入 `Register`、`SignIn`、`ForgotPassword` 或 `Generic`。(`usageType` 是 Logto 用於識別用途的屬性。) 若要啟用完整使用者流程，需提供 `Register`、`SignIn`、`ForgotPassword` 與 `Generic` 這四種 usageType 的範本。

### 測試阿里雲 DM 連接器 \{#test-aliyun-dm-connector}

你可以輸入一個電子郵件地址並點擊「發送」，在「儲存並完成」前測試設定是否可用。

就是這麼簡單。別忘了 <MainSiteUrl href="/connectors/email-connectors/#enable-email-address-for-end-user-flows">在註冊與登入流程中啟用連接器</MainSiteUrl>。

### 設定類型 \{#config-types}

| Name            | Type              |
| --------------- | ----------------- |
| accessKeyId     | string            |
| accessKeySecret | string            |
| accountName     | string            |
| fromAlias       | string (OPTIONAL) |
| templates       | Template[]        |

| Template Properties | Type        | Enum values                                             |
| ------------------- | ----------- | ------------------------------------------------------- |
| subject             | string      | N/A                                                     |
| content             | string      | N/A                                                     |
| usageType           | enum string | 'Register' \| 'SignIn' \| 'ForgotPassword' \| 'Generic' |
