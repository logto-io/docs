---
slug: /quick-starts/nuxt
sidebar_label: Nuxt
sidebar_custom_props:
  description: Nuxt 是一個開源框架，讓網頁開發直覺且強大。
language: js
official_link: https://nuxt.com
app_type: 傳統網頁
framework: Nuxt
---

import FurtherReadings from '../../fragments/_further-readings.md';

import GetUserInformation from './_get-user-information.mdx';
import GuideTip from './_guide-tip.md';
import Installation from './_installation.mdx';
import Integration from './_integration.mdx';
import ApiResources from './api-resources/_index.mdx';

# 為你的 Nuxt 3 應用程式新增驗證 (Authentication)

<GuideTip />

## 先決條件 \{#prerequisites}

- 一個 [Logto Cloud](https://cloud.logto.io) 帳號或 [自行架設 Logto](/introduction/set-up-logto-oss)。
- 已建立的 Logto 傳統網頁應用程式。

## 安裝 \{#installation}

<Installation />

## 整合 \{#integration}

<Integration />

## 取得使用者資訊 \{#get-user-information}

<GetUserInformation />

## API 資源與組織 \{#api-resources-and-organizations}

<ApiResources />

## 用於 middleware 或 API 路由 \{#use-in-middleware-or-api-routes}

`useLogtoClient()` 與 `useLogtoUser()` 這兩個 composable 無法在 middleware 或 API 路由中使用。你可以使用 `logtoEventHandler()` 函式來取得 Logto client 及其他 context：

```ts title="/server/api/auth/get-access-token.ts"
import { logtoEventHandler } from '#logto';

export default defineEventHandler(async (event) => {
  const config = useRuntimeConfig(event);
  await logtoEventHandler(event, config);
  const accessToken = await event.context.logtoClient.getAccessToken();
  return { accessToken };
});
```

## 延伸閱讀 \{#further-readings}

<FurtherReadings />
