<p>
  讓我們切換到 Logto Console 的應用程式詳細資訊頁面。新增一個登出後重新導向 URI{' '}
  <code>{props.postSignOutRedirectUri}</code>，然後點擊「儲存變更」。
</p>

<img alt="Logto Console 中的登出後重新導向 URI" src={props.figureSrc} width="600px" />

[登出後重新導向 URI](https://openid.net/specs/openid-connect-rpinitiated-1_0.html#RPLogout) 是一個 OAuth 2.0 概念，表示登出後應重新導向的位置。
