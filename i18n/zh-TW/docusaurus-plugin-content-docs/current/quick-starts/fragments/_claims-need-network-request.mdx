{props.type ? null : (() => {throw new Error(`props.type is required when importing _claims-need-network-request.mdx`)})()}

### 需要網路請求的宣告 (Claims) \{#claims-that-need-network-requests}

為了防止 ID 權杖 (ID token) 膨脹，某些宣告 (Claims) 需要透過網路請求來獲取。例如，即使在權限範圍 (Scopes) 中請求了 `custom_data` 宣告，它也不會包含在使用者物件中。要存取這些宣告，<span>{props.type === 'method' && (<>你可以使用 <code>{props.method}</code> 方法</>)}{props.type === 'option' && <>你可以配置 <code>{props.configOption}</code> 選項</>}</span>：

{props.codeSnippet}

{props.type === 'method' && (

<>
  此方法將透過請求{' '}
  <a href="https://openid.net/specs/openid-connect-core-1_0.html#UserInfo">userinfo 端點</a>
  來獲取使用者資訊。要了解更多可用的權限範圍 (Scopes) 和宣告 (Claims)，請參閱{' '}
  <a href="#scopes-and-claims">權限範圍 (Scopes) 和宣告 (Claims)</a> 部分。
</>
)}

{props.type === 'option' && (

<>
  通過配置 <code>{props.configOption}</code>，SDK 將在使用者登入後透過請求{' '}
  <a href="https://openid.net/specs/openid-connect-core-1_0.html#UserInfo">userinfo 端點</a>{' '}
  來獲取使用者資訊，並且一旦請求完成，<code>{props.value ?? 'user.custom_data'}</code> 將可用。
</>
)}
