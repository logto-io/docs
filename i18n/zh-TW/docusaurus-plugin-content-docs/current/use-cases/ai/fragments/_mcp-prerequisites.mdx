import Mermaid from '@theme/Mermaid';

## 先決條件 \{#prerequisites}

- 一個 [Logto Cloud](https://cloud.logto.io)（或自託管）租戶
- Node.js >= 20 執行環境

### 瞭解架構 \{#understanding-the-architecture}

- **MCP 伺服器 (MCP server)**：對 MCP 用戶端 (MCP clients) 提供工具與資源的伺服器。根據[最新 MCP 規範](https://modelcontextprotocol.io/specification/latest/basic/authorization)，它作為 OAuth 2.0 資源伺服器，驗證由 Logto 簽發的存取權杖 (Access token)。
- **MCP 用戶端 (MCP client)**：用於啟動驗證流程並測試整合的用戶端。{<b>{props.isThirdParty ? '本指南將以第三方 AI agent 作為用戶端。' : "本指南將以 VS Code（內建 MCP 支援）作為用戶端。"} </b>}
- **Logto**：作為 OpenID Connect 提供者（授權伺服器），管理使用者身分，並為你的 MCP 伺服器簽發受眾綁定的 JWT 存取權杖 (Access token)。

以下是一個非標準的時序圖，說明整體流程：

<Mermaid value={`sequenceDiagram
    participant Client as MCP 用戶端 (MCP Client)<br/>${props.isThirdParty ? '(第三方 AI agent)' : '(VS Code)'}
    participant Server as MCP 伺服器 (MCP Server)<br/>(資源伺服器)
    participant Logto as Logto<br/>(授權伺服器)

    Client->>Server: MCP 請求（無權杖）
    Server->>Client: 401 未授權 (Unauthorized)（WWW-Authenticate）
    Client->>Server: 取得受保護資源中繼資料<br/>(/.well-known/oauth-protected-resource)
    Server->>Client: 回傳包含授權伺服器資訊的中繼資料
    Client->>Logto: 取得授權伺服器中繼資料
    Logto->>Client: 回傳 OpenID Connect 設定
    Client->>Logto: 導向 Logto 進行驗證
    Logto->>Logto: 使用者驗證
    ${props.isThirdParty ? 'Logto->>Logto: 使用者授權（同意）權限範圍 (Scopes)<br/>給第三方 AI agent' : ''}
    Logto->>Client: 驗證碼導回
    Client->>Logto: 以驗證碼換取存取權杖 (Access token)<br/>（帶上 resource 參數）
    Logto->>Client: 回傳存取權杖 (JWT，受眾綁定)
    Client->>Server: 帶存取權杖的 MCP 請求
    Server->>Server: 依 Logto 的 JWKS 驗證存取權杖<br/>（簽章、簽發者 (Issuer)、受眾 (Audience)、過期時間）
    Server->>Client: 回傳工具回應`}

/>

:::note
由於 MCP 發展迅速，上述圖表可能未完全反映最新狀態。請參考 [mcp-auth](https://mcp-auth.dev) 文件以取得最新資訊。
:::
