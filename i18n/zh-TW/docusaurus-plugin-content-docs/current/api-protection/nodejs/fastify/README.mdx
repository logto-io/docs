---
sidebar_label: Fastify
---

import AddValidationLogic from '../../../authorization/validate-access-tokens/nodejs/_add-validation-logic.mdx';
import Init from '../../../authorization/validate-access-tokens/nodejs/_init.mdx';
import ApplyMiddleware from '../../../authorization/validate-access-tokens/nodejs/fragments/fastify/_apply-middleware.md';
import MainContent from '../../fragments/_main-content.mdx';

import InitProject from './_init-project.mdx';

# 使用 RBAC（角色型存取控制 (Role-based access control)）與 JWT（JSON Web Token）驗證保護你的 Fastify API

本指南將協助你透過 [角色型存取控制 (RBAC, Role-based access control)](/authorization/role-based-access-control) 以及由 Logto 簽發的 [JSON Web Token (JWT)](https://auth.wiki/jwt)，為你的 Fastify API 實作授權 (Authorization) 機制，確保 API 安全。

{/* 警告：標題或導言文字中無法使用變數，否則將無法正確渲染。 */}

<MainContent
  framework="fastify"
  initializeProject={<InitProject />}
  languageInit={<Init />}
  applyMiddleware={<ApplyMiddleware />}
  addValidationLogic={<AddValidationLogic framework="fastify" />}
/>
