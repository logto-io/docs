---
sidebar_label: Symfony
---

import AddValidationLogic from '../../../authorization/validate-access-tokens/php/_add-validation-logic.mdx';
import Init from '../../../authorization/validate-access-tokens/php/_init.mdx';
import ApplyMiddleware from '../../../authorization/validate-access-tokens/php/fragments/symfony/_apply-middleware.md';
import MainContent from '../../fragments/_main-content.mdx';

import InitProject from './_init-project.mdx';

# 使用 RBAC 與 JWT 驗證 (JWT validation) 保護你的 Symfony API

本指南將協助你透過 [角色型存取控制 (RBAC, Role-based access control)](/authorization/role-based-access-control) 以及由 Logto 簽發的 [JSON Web Token (JWT)](https://auth.wiki/jwt)，為你的 Symfony API 實作授權 (Authorization) 機制。

{/* WARNING: We cannot use variables in the headings or introductory text since it will not be rendered correctly. */}

<MainContent
  framework="symfony"
  initializeProject={<InitProject />}
  languageInit={<Init />}
  applyMiddleware={<ApplyMiddleware />}
  addValidationLogic={<AddValidationLogic framework="symfony" />}
/>
