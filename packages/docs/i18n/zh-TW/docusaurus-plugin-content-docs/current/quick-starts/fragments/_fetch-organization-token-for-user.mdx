如果你對組織 (Organization) 不熟悉，請閱讀 [🏢 組織（多租戶，Multi-tenancy）](/organizations) 以開始了解。

在配置 Logto client 時，你需要新增 <code>{props.organizationScope}</code> 權限範圍 (scope)：

{props.configOrganizationCode}

使用者登入後，你可以為使用者獲取組織權杖 (organization token)：

{props.getOrganizationAccessTokenCode}
