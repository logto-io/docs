{props.type ? null : (() => {throw new Error(`props.type is required when importing _claims-need-network-request.mdx`)})()}

### Reivindicações que precisam de solicitações de rede \{#claims-that-need-network-requests}

Para evitar o inchaço do Token de ID (ID token), algumas reivindicações requerem solicitações de rede para serem buscadas. Por exemplo, a reivindicação `custom_data` não está incluída no objeto do usuário, mesmo que seja solicitada nos escopos. Para acessar essas reivindicações, <span>{props.type === 'method' && (<>você pode usar o método <code>{props.method}</code></>)}{props.type === 'option' && <>você pode configurar a opção <code>{props.configOption}</code></>}</span>:

{props.codeSnippet}

{props.type === 'method' && (

<>
  Este método buscará as informações do usuário solicitando ao{' '}
  <a href="https://openid.net/specs/openid-connect-core-1_0.html#UserInfo">endpoint userinfo</a>.
  Para saber mais sobre os escopos e reivindicações disponíveis, veja a seção{' '}
  <a href="#scopes-and-claims">Escopos e reivindicações</a>.
</>
)}

{props.type === 'option' && (

<>
  Ao configurar <code>{props.configOption}</code>, o SDK buscará as informações do usuário
  solicitando ao{' '}
  <a href="https://openid.net/specs/openid-connect-core-1_0.html#UserInfo">endpoint userinfo</a>{' '}
  após o usuário fazer login, e <code>{props.value ?? 'user.custom_data'}</code> estará disponível
  assim que a solicitação for concluída.
</>
)}
