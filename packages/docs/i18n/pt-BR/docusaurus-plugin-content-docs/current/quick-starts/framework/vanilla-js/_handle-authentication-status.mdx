No Logto SDK, geralmente podemos usar `logtoClient.isAuthenticated` para verificar o status de autenticação. Se o usuário estiver autenticado, o valor será `true`; caso contrário, o valor será `false`.

No seu aplicativo JS puro, você pode usar o status `isAuthenticated` para mostrar e ocultar programaticamente os botões de login e logout. Vamos ver como fazer isso.

```js
const redirectUrl = 'http://localhost:3000/callback';
const baseUrl = 'http://localhost:3000';

// Renderização condicional dos botões de login e logout
const render = async (logtoClient) => {
  const isAuthenticated = await logtoClient.isAuthenticated();
  const container = document.querySelector('#container');

  const onClickSignIn = () => logtoClient.signIn(redirectUrl);
  const onClickSignOut = () => logtoClient.signOut(baseUrl);

  const button = document.createElement('button');
  button.innerHTML = isAuthenticated ? 'Sign Out' : 'Sign In';
  button.addEventListener('click', isAuthenticated ? onClickSignOut : onClickSignIn);

  container.append(button);
};
```
