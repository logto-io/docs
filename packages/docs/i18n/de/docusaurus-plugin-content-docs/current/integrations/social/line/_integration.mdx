### Einrichten eines Line-Kanals \{#setup-a-line-channel}

Gehe zu den [Line Developers](https://developers.line.biz/console/) und melde dich mit deinem Line-Geschäftskonto an. Wenn du noch kein Konto hast, kannst du eines registrieren.

Gehe dann zu [diesem Link](https://developers.line.biz/console/register/line-login/provider/), um einen Kanal zu erstellen.

**Schritt 1:** Fülle die Kanaldetails aus.

Vervollständige das Formular und erstelle den Kanal.

**Schritt 2:** Richte die Callback-URLs ein.

Gehe zur Kanaldetailseite, finde den Tab "LINE login" und bearbeite das Feld "Callback URL".

In unserem Fall wird dies `${your_logto_endpoint}/callback/${connector_id}` sein, z. B. `https://foo.logto.app/callback/${connector_id}`. Die `connector_id` kann in der oberen Leiste der Logto Admin Console auf der Connector-Detailseite gefunden werden.

### Konfiguriere deinen Connector \{#configure-your-connector}

In deiner Logto-Connector-Konfiguration fülle die folgenden Felder mit den Werten aus, die du im "Auth"-Tab, Abschnitt "Application credentials" deiner App erhalten hast:

- **clientId:** Die Channel-ID deiner App.
- **clientSecret:** Das Channel-Secret deiner App.

`scope` ist eine durch Leerzeichen getrennte Liste von OIDC-Berechtigungen. Wenn nicht angegeben, ist die Standardberechtigung `openid profile`.

### Konfigurationstypen \{#config-types}

| Name         | Typ    |
| ------------ | ------ |
| clientId     | string |
| clientSecret | string |
| scope        | string |
