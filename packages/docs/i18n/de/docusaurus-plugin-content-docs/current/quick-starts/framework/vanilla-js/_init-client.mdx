Importiere und initialisiere eine `LogtoClient`-Instanz, indem du die Konfiguration übergibst:

```ts title="index.js"
import LogtoClient from '@logto/browser';

const logtoClient = new LogtoClient({
  endpoint: '<your-logto-endpoint>',
  appId: '<your-application-id>',
});
```

Der `endpoint` und die `appId` sind auf der Anwendungsdetailseite in der Logto-Konsole zu finden.
